# Base URL of the DealRoom backend proxy.
# Local development matches backend/.env.example PORT (3001).
# Set this to your deployed Render or Railway URL in production.
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
